
<%= form_for([@post, @post.comments.build], class:"form-horizontal") do |f| %>
<% if @post.comments.errors.any? %>
    <div id="error_explanation">
      <h6><%= pluralize(@post.comments.errors.count, "error") %> prohibited this post from being saved:</h6>

      <ul>
      <% @post.comments.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-lg-6">
    <div class="well well-sm">
      <div class="row">
        <div class="col-lg-12">
    <div class="field form-group">
    <%= f.label :Your_Name, class: 'col-sm-3 text-right control-label' %>
    <div class="col-sm-9">
      <%= f.text_field :commenter , class: 'form-control' %>
    </div>
  </div><br>
  <div class="field form-group">
    <%= f.label :Comment, class: 'col-sm-3 text-right control-label' %>
    <div class="col-sm-9">
      <%= f.text_area :body, class: 'form-control' %>
    </div>
  </div>
  <div class="actions form-group">
    <div class="col-sm-offset-3 col-sm-9">
      <%= f.submit :SUBMIT, class: 'btn btn-default btn-xs' %>
    </div>
  </div>
  </div>
</div>
</div>
</div>
</div>


 
<% end %>


